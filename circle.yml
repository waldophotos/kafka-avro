machine:
  pre:
   - pip install docker-compose
  services:
    - docker
  node:
    version: 8.2.0

dependencies:
  pre:
    - docker-compose up -d
    - sudo apt-get update;
    - sudo apt-get -y install libsasl2-dev libssl-dev
